# MLOps Practitioner — Mini Project 1

A production-oriented wildfire classification service using a Random Forest model to predict `fire` or `no_fire` from three environmental features: `NDVI`, `LST`, and `BURNED_AREA`.
